name: "RepoB Action"
description: "Intermediate action interacting with RepoC"

inputs:
  token:
    description: "GitHub Token for accessing RepoC"
    required: true

runs:
  using: "composite"
  steps:
    - name: Use RepoC Action
      uses: 0xhellhound/repoC@main
      with:
        token: ${{ inputs.token }}
      env:
        SECRET_VAR: ${{ secrets.DAMN_SECRET }}

    - name: Dummy Step
      run: echo "You've been pwned!"
      shell: bash
